public class Main {

    /*
     * By default if the constructor isn't specified, Java will generate constructor
     * automatically know as the default constructor.
     */
    public static void main(String[] args) {

        // Instantiate using the default constructor
        Main main = new Main();
    }
}
